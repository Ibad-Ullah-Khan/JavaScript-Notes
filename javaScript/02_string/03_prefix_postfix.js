/*
1. Prefix (++x)
The variable is updated first, then its value is used.
*/

let x = 5;
console.log(++x);

/*
2. Postfix (x++)
The current value is used first, then the variable is updated.
*/

let y = 5;
console.log(y++);